در کد ما یک ماتریس به صورت لیست دو بعدی به نام mylist داریم, که به دلیل وجود داشتن مقادیر 0 زیاد, این ماتریس اسپارس است که میخواهیم
با استفاده از تابع my_sparse آن را بهینه کنیم تا فقط خانه هایی که مقادیر غیر صفر دارند را به صورت یک لیست دو بعدی سه ستونه خروجی دهند
 که در هر سطر: مقدار اول سطر, مقدار دوم ستون و مقدار سوم مقدار آن عدد باشند.
در تابع my_sparse در خط دوم تابع, خروجیمان را به صورت یک لیست یک بعدی به نام  my_efficiant_list تعریف میکنیم.
سپس با استفاده از توابع ()len و ()range مقدار سطر ها را درون i و مقدار ستون ها را درون j میریزیم.
بعد از آن, شرط میگذاریم که اگر مقدار لیست ما 0 نباشد سطر, ستون و مقدار آن را به صورت یک لیست درون لیست  my_efficiant_list قرار دهد.
و با اینکار تابع  my_efficiant_list هم دو بعدی میشود.
و در آخر لیست  my_efficiant_list را return میکنیم.